# deps
node_modules/

# next / opennext / wrangler build output
.next/
.open-next/
.wrangler/
out/

# env & secrets — wildcard catches any .env.* file with credentials
.env*
!.env.example
.dev.vars
.dev.vars.*

# misc
.DS_Store
*.log
tsconfig.tsbuildinfo
next-env.d.ts

# QA/session artifacts
/*.png
.playwright-mcp/
# local-only demo seed (fake engagement data — never commit/apply to prod)
seed-demo.sql
